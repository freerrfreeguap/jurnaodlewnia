# Supabase client config (Vite exposes these to the browser bundle)
VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_URL=""

# Supabase edge function secrets (set via `supabase secrets set`, not read from this file):
# HCAPTCHA_SECRET
# RESEND_API_KEY
